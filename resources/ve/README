This is a copy of a library from VisualEditor.

It is duplicated here because, for MediaWiki versions before 1.32,
it is not included in the MediaWiki module of the VisualEditor extension.

Starting in MediaWiki 1.32, ve.init.mw.Target can be used instead; and
starting in version 1.34, ve.init.sa.Target can no longer even be used
because it is not fully compatible with latest VisualEditor.

This file (and directory) will be removed once support for MediaWiki < 1.32
is dropped.